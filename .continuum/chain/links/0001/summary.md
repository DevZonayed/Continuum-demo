# Link 0001 — bootstrap

Initial Continuum bootstrap for the `Continuum-demo` repo. Working tree is populated but has zero git commits on `master`. The project is a small Node.js (ESM) Express service with JWT auth (`jsonwebtoken` + `bcrypt`) and a JSON-file store at `data/db.json`. Entrypoint is `index.js`, which starts the Express app from `src/app.js`; routes are split into `/auth` (public) and `/todos` (JWT-guarded via `src/middleware/jwtAuth.js`). Configuration comes from `src/config.js`, env-driven (`PORT`, `JWT_SECRET`, `DB_PATH`) with dev fallbacks; `.env.example` documents the variables and `.gitignore` excludes `.env` and `node_modules/`. No tests, no CI, no decisions logged yet. STATE.md captures the current shape and lists open threads (initial commit, unexamined auth/todos/store internals, missing test suite) so the next session can pick up cleanly.
